'use client';

import { AdminShell } from '@/admin/shell';

export default function UsersPage() {
	return (
		<AdminShell>
			<h1 className="font-display text-[2rem] leading-tight">Users</h1>
			<p className="prose-body text-muted mt-4 text-base">
				Not built yet. The Flutter admin had this route too, where it rendered a placeholder — it is
				carried over as a placeholder rather than quietly dropped from the navigation.
			</p>
		</AdminShell>
	);
}
